import 'package:flutter/material.dart';
class D extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text(
              "D screen",style: TextStyle(color: Colors.blue,fontSize: 20,fontWeight: FontWeight.normal))),
    );
  }
}
